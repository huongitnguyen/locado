{
  "name": "locado-landing",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n astro,wrangler -c blue,yellow \"astro dev\" \"wrangler pages dev ./dist --port 8788\"",
    "dev:astro": "astro dev",
    "dev:wrangler": "wrangler pages dev ./dist --port 8788",
    "dev:cf": "npm run build && wrangler pages dev ./dist",
    "build": "astro build",
    "preview": "npm run build && wrangler pages dev ./dist",
    "deploy": "npm run build && wrangler pages deploy ./dist"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^6.0.0",
    "@cloudflare/workers-types": "^4.20260120.0",
    "concurrently": "^9.2.1",
    "tailwindcss": "^3.4.0",
    "wrangler": "^4.59.1"
  }
}
